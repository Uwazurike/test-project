@model IEnumerable<TaskInsider.InsiderSkill>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Insider.UserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Skill.SkillName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SkillLevel.SkillLevelName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Insider.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Skill.SkillName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SkillLevel.SkillLevelName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InsiderSkillID }) |
            @Html.ActionLink("Details", "Details", new { id=item.InsiderSkillID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InsiderSkillID })
        </td>
    </tr>
}

</table>
