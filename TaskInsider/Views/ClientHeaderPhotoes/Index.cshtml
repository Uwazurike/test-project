@model IEnumerable<TaskInsider.ClientHeaderPhoto>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HeaderFileName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Client.UserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HeaderFileName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Client.UserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ClientHeaderPhotoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ClientHeaderPhotoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ClientHeaderPhotoID })
        </td>
    </tr>
}

</table>
