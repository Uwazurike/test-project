@model IEnumerable<TaskInsider.SkillLevel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SkillLevelName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SkillLevelName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SkillLevelID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SkillLevelID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SkillLevelID })
        </td>
    </tr>
}

</table>
