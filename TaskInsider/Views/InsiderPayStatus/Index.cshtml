@model IEnumerable<TaskInsider.InsiderPayStatu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InsiderPayStatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.InsiderPayStatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InsiderPayStatusID }) |
            @Html.ActionLink("Details", "Details", new { id=item.InsiderPayStatusID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InsiderPayStatusID })
        </td>
    </tr>
}

</table>
