@model IEnumerable<TaskInsider.ClientProfileModule>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClientHeaderPhoto.HeaderFileName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ClientPhoto.PhotoFileName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ClientProfile.ClientProfileID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClientHeaderPhoto.HeaderFileName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ClientPhoto.PhotoFileName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ClientProfile.ClientProfileID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ClientProfileModuleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ClientProfileModuleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ClientProfileModuleID })
        </td>
    </tr>
}

</table>
