@model IEnumerable<TaskInsider.AddressType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AddressType1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AddressType1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AddressTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AddressTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AddressTypeID })
        </td>
    </tr>
}

</table>
