@model IEnumerable<TaskInsider.ClientPaymentStatu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClientPaymentStatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClientPaymentStatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ClientPaymentStatusID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ClientPaymentStatusID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ClientPaymentStatusID })
        </td>
    </tr>
}

</table>
