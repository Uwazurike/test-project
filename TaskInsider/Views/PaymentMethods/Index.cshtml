@model IEnumerable<TaskInsider.PaymentMethod>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PaymentMethod1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PaymentMethod1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PaymentMethodID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PaymentMethodID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PaymentMethodID })
        </td>
    </tr>
}

</table>
