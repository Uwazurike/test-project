@model IEnumerable<TaskInsider.RequestStatu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RequestStatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RequestStatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RequestStatusID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RequestStatusID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RequestStatusID })
        </td>
    </tr>
}

</table>
